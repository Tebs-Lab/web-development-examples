window.onload = function() {
    let paragraph = document.createElement('p');
    paragraph.innerText = "This wasn't in the HTML!";
    document.body.appendChild(paragraph);

    // Micro-exercise: create a header tag and append it to the DOM!
};
  